<!DOCTYPE html>
<html lang="ja">
<head>
	<meta charset="UTF-8">
	<title>MUSIC SNS</title>
	<link rel="stylesheet" href="/assets/css/index.css">
	<link href="https://fonts.googleapis.com/css?family=Righteous" rel="stylesheet">
<body>
<nav class="center-hrizontal">
	<a href="/">MUSIC SNS!!!</a>
	<% if session[:user] %>
		<a href="#"><img src="<%= User.find(session[:user]).image_url %>" alt="user_image" width="40" height="40"></a></li>
		<a href="#">Search</a></li>
		<a href="/signout">Sign out</a>
	<% else %>
		<a href="/signup">Sign up</a>
	<% end %>
</nav>
<main>

<%= yield %>

</main>
</body>
</html>